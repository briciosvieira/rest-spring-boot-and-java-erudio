cors:
  originPatterns: http://localhost:3000,http://localhost:8080,https://briciosvieira.com
security:
  jwt:
    token:
      secret-key: curso
      expire-lenght: 360

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/java_erudio?useTimezone=true&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
server:
  port: 8082
